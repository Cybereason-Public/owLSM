{{- $json := "" -}}
{{- if .Values.configJson -}}
{{- $json = .Values.configJson -}}
{{- else -}}
{{- $cfg := deepCopy .Values.config -}}
{{- $_ := set $cfg.kubernetes "cri_endpoint" (printf "unix://%s" .Values.socketHostPath) -}}
{{- $json = $cfg | toPrettyJson -}}
{{- end -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: owlsm-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "owlsm.labels" . | nindent 4 }}
data:
  config.json: |
    {{- $json | nindent 4 }}
